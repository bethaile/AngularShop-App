export class Product {
    constructor(
        public id: number,
        public brand: string,
        public name: string,
        public price: number,
        public description: string
      ) {  }
}